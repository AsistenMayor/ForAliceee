<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Untuk Kamu yang Masih Aku Cintai</title>
  <style>
    body {
      margin: 0;
      background: linear-gradient(180deg, #ffeaf3, #fff);
      font-family: "Poppins", sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
      color: #333;
    }
    .card {
      background: rgba(255,255,255,0.8);
      padding: 32px;
      border-radius: 18px;
      text-align: center;
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
      width: 90%;
      max-width: 480px;
      animation: fadeIn 2s ease forwards;
    }
    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }
    h1 {
      color: #ff6fa3;
      margin-bottom: 16px;
    }
    p {
      font-size: 18px;
      line-height: 1.6;
    }
    button {
      margin-top: 22px;
      padding: 12px 20px;
      border: none;
      background: #ff6fa3;
      color: white;
      border-radius: 10px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      background: #ff88b0;
      transform: scale(1.05);
    }
    .heart {
      position: absolute;
      animation: floatUp 5s linear infinite;
    }
    @keyframes floatUp {
      0% {transform: translateY(0) scale(0.8); opacity: 0;}
      20% {opacity: 1;}
      100% {transform: translateY(-600px) scale(1.2); opacity: 0;}
    }
    audio {display: none;}
  </style>
</head>
<body>
  <audio autoplay loop>
    <source src="https://dl.dropboxusercontent.com/scl/fi/x8b8r7j6r8x6h6z3x5t6n/Too-Good-To-Say-Goodbye.mp3" type="audio/mpeg">
  </audio>

  <div class="card">
    <h1>Untuk Kamu 💌</h1>
    <p>Kalau aku boleh minta satu hal aja… aku cuma mau kamu percaya lagi.  
    Aku sadar, kata maaf aja nggak cukup, tapi aku janji bakal buktiin lewat tindakan, bukan cuma janji.</p>
    <button onclick="nextMessage()">Lihat lagi 👉</button>
  </div>

  <script>
    const messages = [
      "Aku tau aku pernah nyakitin kamu, dan aku gak akan nyari alasan.",
      "Aku cuma pengen kesempatan kecil buat buktiin kalau aku bisa lebih baik.",
      "Kamu itu rumah paling hangat buat hatiku.",
      "Jadi... apa aku boleh pulang lagi ke hati kamu? 💖"
    ];
    let i = 0;
    function nextMessage() {
      const card = document.querySelector('.card p');
      if (i < messages.length) {
        card.innerHTML = messages[i];
        i++;
      } else {
        card.innerHTML = "Terima kasih udah baca sampai sini. Semoga kamu bisa percaya lagi sama aku. 🌷";
      }
    }

    // Floating hearts
    function createHeart() {
      const heart = document.createElement('div');
      heart.innerHTML = "💗";
      heart.classList.add('heart');
      heart.style.left = Math.random() * 100 + "%";
      heart.style.fontSize = Math.random() * 20 + 10 + "px";
      heart.style.animationDuration = 4 + Math.random() * 3 + "s";
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 7000);
    }
    setInterval(createHeart, 500);
  </script>
</body>
</html>
